<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="Turbo Bar"
        android:allowBackup="false">

        <service
            android:name=".TurboBarIME"
            android:label="Turbo Bar"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!--
          Required for commitContent()/image insertion: the receiving app
          needs a content:// URI, not a raw file path, to be able to read
          the QR/image bytes we generate. FileProvider is the standard
          Android mechanism for handing another app temporary, scoped read
          access to a file this app owns.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
